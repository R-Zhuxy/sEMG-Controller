# 项目计划书：基于单通道 sEMG 的体感交互控制系统

## 一、 整体项目概述
本项目旨在设计并开发一套基于表面肌电信号（sEMG）的非侵入式人机交互系统（大作业原型验证）。将传统的硬件阈值判定升级为基于数字信号处理 (DSP) 的包络提取与动态触发逻辑。系统通过贴附在小臂肌肉表面的单通道肌电传感器采集肌肉收缩时的微弱生物电信号，经微控制器进行模数转换并发送至上位机。上位机（Python）接收数据后，运行数字信号处理算法提取肌肉发力特征，并将其映射为电脑底层的键盘按键指令，最终实现“无实物”控制电脑的功能。
* **医学应用背景预想**：为重度肢体运动障碍患者（如截肢、高位截瘫但保留部分局部肌肉控制能力的患者）提供新型的电脑辅助交互接口 (HCI)。
* **本项目原型产品希望达到的场景**：视觉反馈强、且无需底层驱动级按键注入的轻量级场景。


## 二、 功能列表
1.  **生理信号采集**：实时采集前臂肌肉（如指浅屈肌或肱桡肌）的 sEMG 原始模拟信号。
2.  **数据传输**：以稳定的采样率（如 500Hz-1000Hz）通过串口将数据传输至 PC。
3.  **实时信号清洗与预处理**：在软件端过滤工频干扰及环境基线噪声
4.  **发力特征提取**：计算肌电信号的包络线（RMS 或积分绝对值）以及发力区间（Onsets/Offsets）。
5.  **自适应动态校准**：系统启动时自动读取用户的静息肌电水平，建立个性化阈值；启动时自动标定背景噪声，计算信噪比 (SNR)。
6.  **动作意图分类**：精准识别“放松”与“爆发发力”两种状态，并具备防抖逻辑。
7.  **虚拟按键映射**：将识别到的“发力”状态转换为操作系统的系统级按键注入（如按下空格键等），将分析结果映射为系统级键盘事件，控制第三方软件或游戏。
8.  **可视化数据面板**：实时绘制原始肌电波形和处理后的包络线，同步展示原始电位图与算法处理后的特征曲线。

## 三、 技术栈选择
* **硬件端（已经购买，不可更改）**：
    * 微控制器：**Arduino Nano  V3 CH340 Atmega328P**。
    * 传感器：**模拟单通道干电极sEMG表面肌电模采集模块 (思知瑞)**
* **软件端（上位机）初步设想**：
    * 核心语言：**Python 3.14**。
    * 串口通信：`pyserial`。
    * 信号处理与数学运算：`numpy`, `scipy`, `NeuroKit2` 等。
    * 系统控制（键盘映射）：`pyautogui`等。
    * 可视化界面（后期）：`pyqtgraph`, `PyQt5` 等。
* **软件架构补充**：
    * 数据层优化：引入“环形缓冲区” (Ring Buffer)，控制内存消耗、保持系统运行流畅。
    * 算法层的状态判定：不使用单阈值，引入“施密特触发器”逻辑 (Schmitt Trigger Logic)，避免抖动和误触引发的问题。

## 四、 功能模块和交互关系
整个系统呈现清晰的流水线（Pipeline）结构【初步设想与举例】：
1.  **【物理层】肌肉收缩** -> 产生体表电位差。
2.  **【硬件层】传感器模块** -> 放大微小信号，输出 0-5V 模拟电压。
3.  **【固件层】Arduino** -> ADC 模数转换（将电压变数字 0-1023），通过 USB 串口以字符串形式持续打印。
4.  **【软件通信层】Python 串口读取** -> 开启独立线程读取串口缓冲，存入数据队列。
5.  **【算法层】Python 信号处理** -> 对队列数据执行：去均值 -> 绝对值整流 -> 滑动窗口平滑（得出包络线） -> 阈值对比。
6.  **【执行层】Python 动作注入** -> 若包络线突破阈值，触发 `pyautogui.press('space')`。

## 五、要求
- 所有项目架构和代码都在本文件夹下构建
- 需创建名为 `semgvenv` 的虚拟环境并在其中安装所有所需的依赖
- 所有的目录结构和项目架构建立需符合工业标准