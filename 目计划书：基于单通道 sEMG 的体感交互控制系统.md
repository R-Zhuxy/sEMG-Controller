# 项目计划书：基于单通道 sEMG 的体感交互控制系统

## 一、 整体项目概述
本项目旨在设计并开发一套基于表面肌电信号（sEMG）的非侵入式人机交互系统。系统通过贴附在小臂肌肉表面的单通道肌电传感器采集肌肉收缩时的微弱生物电信号，经微控制器进行模数转换并发送至上位机。上位机（Python）接收数据后，运行数字信号处理算法提取肌肉发力特征，并将其映射为电脑底层的键盘按键指令，最终实现“无实物”控制电脑游戏（如《恐龙快跑》）的功能。
* **医学应用背景**：为重度肢体运动障碍患者（如截肢、高位截瘫但保留部分局部肌肉控制能力的患者）提供新型的电脑辅助交互接口 (HCI)。

## 二、 功能列表
1.  **生理信号采集**：实时采集前臂肌肉（如指浅屈肌或肱桡肌）的 sEMG 原始模拟信号。
2.  **数据传输**：以稳定的采样率（如 500Hz）通过串口将数据传输至 PC。
3.  **实时信号清洗**：在软件端过滤工频干扰及环境基线噪声。
4.  **发力特征提取**：计算肌电信号的包络线（RMS 或积分绝对值）。
5.  **自适应动态校准**：系统启动时自动读取用户的静息肌电水平，建立个性化阈值。
6.  **动作意图分类**：精准识别“放松”与“爆发发力”两种状态，并具备防抖逻辑。
7.  **虚拟按键映射**：将识别到的“发力”状态转换为操作系统的系统级按键注入（如按下空格键等）。
8.  **可视化数据面板**：实时绘制原始肌电波形和处理后的包络线，用于展示。

## 三、 技术栈选择
* **硬件端**：
    * 微控制器：**Arduino Nano  V3 CH340 Atmega328P**。
    * 传感器：**模拟单通道干电极sEMG表面肌电模采集模块**
* **软件端（上位机）**：
    * 核心语言：**Python 3.14**。
    * 串口通信：`pyserial`。
    * 信号处理与数学运算：`numpy`, `scipy`等。
    * 系统控制（键盘映射）：`pyautogui`等。
    * 可视化界面（后期）：`pyqtgraph`等。

## 四、 功能模块和交互关系
整个系统呈现清晰的流水线（Pipeline）结构：
1.  **【物理层】肌肉收缩** -> 产生体表电位差。
2.  **【硬件层】传感器模块** -> 放大微小信号，输出 0-5V 模拟电压。
3.  **【固件层】Arduino** -> ADC 模数转换（将电压变数字 0-1023），通过 USB 串口以字符串形式持续打印。
4.  **【软件通信层】Python 串口读取** -> 开启独立线程读取串口缓冲，存入数据队列。
5.  **【算法层】Python 信号处理** -> 对队列数据执行：去均值 -> 绝对值整流 -> 滑动窗口平滑（得出包络线） -> 阈值对比。
6.  **【执行层】Python 动作注入** -> 若包络线突破阈值，触发 `pyautogui.press('space')`。

---

## 五、 详细分步实现计划（Task化）

以下计划按照**优先级从高到低、从底层到上层**排列。完成前三步，你的作业就能保底及格了；完成全部，妥妥的高分。

### Task 1：跑通硬件“有感”，实现原始数据采集
* **目标**：确保传感器能正确读取肌肉变化，Arduino 能稳定发送数据。
* **步骤**：
    1.  查阅传感器手册，将信号引脚连入 Arduino 的 A0 接口，并接好 VCC 和 GND。
    2.  在手臂正确位置（肌腹处两片，无肌肉的骨骼处一片作参考地）贴上电极片。
    3.  编写 Arduino C 语言代码，例如使用 `analogRead(A0)`，配合 `delay` 控制采样率，通过 `Serial.println()` 发送数据。
    4.  打开 Arduino IDE 的“串口绘图器”，用力握拳，观察波形是否有明显起伏。
* **给 AI 的后续 Prompt 示例**：*“我有一个 Arduino Nano 和一个模拟输出的肌电传感器，接在 A0。请帮我写一段 Arduino 代码，以 500Hz 的采样率读取模拟值，并通过串口打印出来，以便我用串口绘图器观察波形。”*

### Task 2：打通软硬连接，Python 串口数据接收
* **目标**：将数据流从 Arduino 转移到电脑的 Python 环境中。
* **步骤**：
    1.  在电脑端安装 Python 及 `pyserial` 库。
    2.  编写 Python 脚本，连接对应的 COM 端口。
    3.  写一个 `while True` 循环，持续读取串口行数据，去除换行符，转换为整数类型并存入一个列表或数组中。
* **给 AI 的后续 Prompt 示例**：*“帮我写一个 Python 脚本，使用 pyserial 库读取 COM3 端口的数据。波特率是 115200。Arduino 传过来的是带有换行符的纯数字字符串，请将其解析为整数并打印在终端里。”*

### Task 3：核心工作量——肌电信号处理与特征提取
* **目标**：把杂乱的上下跳动的毛刺信号，变成一条代表“用力大小”的平滑包络线。
* **步骤**：
    1.  **数据整流**：把接收到的原始数据减去基线值（通常是 ADC 最大值的一半，比如 512），然后取绝对值。
    2.  **滑动平均/RMS**：编写一个长度为 N（如 N=50）的数组作为滑动窗口，计算窗口内数据的平均值或均方根。
    3.  观察终端打印的处理后数据：放松时数值应接近 0，用力握拳时数值应平稳上升到一个高值。
* **给 AI 的后续 Prompt 示例**：*“我现在用 Python 接收到了一维的肌电原始数据流。请帮我写一个数据处理函数：先对数据进行全波整流（减去均值后取绝对值），然后使用滑动窗口算法（窗口大小为 50）计算均方根（RMS），提取出肌电信号的包络线。”*

### Task 4：状态逻辑判定与按键映射（MVP 诞生）
* **目标**：系统能通过肌肉控制电脑打游戏。
* **步骤**：
    1.  引入 `pyautogui` 库。
    2.  设定一个阈值（基于你在 Task 3 中观察到的用力数值）。
    3.  编写判定逻辑：当包络线数值 `> 阈值` 且当前状态为“未按下”时，执行 `pyautogui.press('space')`，并将状态标记为“已按下”；当包络线数值 `< 阈值 * 0.8`（滞回区间防抖）时，重置状态。
    4.  打开 Chrome 浏览器，断网进入“恐龙快跑”页面，测试肌肉控制跳跃。
* **给 AI 的后续 Prompt 示例**：*“我现在有实时计算出的肌电包络线数值。请用 pyautogui 帮我写一段触发控制逻辑：设定一个触发阈值（比如 200）。当数值大于 200 时按下空格键，但必须等数值回落到 150 以下，才能允许下一次触发。请帮我实现这个带状态机防抖的逻辑。”*

### Task 5：自适应基线校准（系统优化，报告亮点）
* **目标**：消除每个用户皮肤阻抗不同带来的误差，让系统更智能。
* **步骤**：
    1.  在程序启动的前 3 秒内，不进行任何按键控制。
    2.  提示用户“保持彻底放松”，记录这 3 秒内的环境噪声均值，作为 `Noise_Base`。
    3.  将触发阈值动态设置为 `Noise_Base + 常数C`（或乘以一个系数）。
* **给 AI 的后续 Prompt 示例**：*“为了增强肌电系统的鲁棒性，我需要在 Python 程序启动的前 3 秒加入‘基线校准’功能。在这个阶段收集数据计算平均噪声水平，然后根据这个噪声水平自动推算出一个合适的触发阈值。请帮我补充这部分逻辑。”*

### Task 6：开发可视化监控面板（决战高分）
* **目标**：做一个有科技感的界面，让老师直观看到你的算法把原始波形变成了什么样。
* **步骤**：
    1.  使用 `pyqtgraph`（推荐，因为渲染实时高频波形不卡顿）。
    2.  界面分为上下两个图表。
    3.  上图表：实时滚动显示蓝色的原始肌电波形 (Raw sEMG)。
    4.  下图表：实时滚动显示红色的包络线 (RMS Envelope)，并在图表上画一根横向的绿色虚线代表“触发阈值”。
* **给 AI 的后续 Prompt 示例**：*“请使用 PyQt5 和 pyqtgraph 帮我写一个实时数据波形显示界面。要求能同时接收两组数据：原始肌电信号和处理后的包络线信号。界面上下排列两个随时间滚动的折线图，上边画原始数据，下边画包络线，并在下方的图表中加入一条代表阈值的水平参考线。”*